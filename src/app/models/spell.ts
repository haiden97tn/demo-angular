export class Spell {
  // id, name, image
  id: Number;
  name: string;
  image: string;

  constructor(id: Number, name: string, image: string){
      this.id = id;
      this.name = name;
      this.image = image;
  }
}
